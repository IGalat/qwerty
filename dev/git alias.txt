git config --global --edit

git config --global alias.

[alias]
	br = branch
	co = checkout
	cm = commit -m
	st = status
	amend = commit --amend --no-edit
	aliases = config --get-regexp ^alias\\.
	




sync = ! [ -z "$1" ] && echo "1" || git repo-sync path-"$1" && :